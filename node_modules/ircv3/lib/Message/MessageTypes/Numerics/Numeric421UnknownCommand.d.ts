import Message, { MessageParam, MessageParamSpec } from '../../Message';
export interface Numeric421UnknownCommandParams {
    me: MessageParam;
    command: MessageParam;
    suffix: MessageParam;
}
export default class Numeric421UnknownCommand extends Message<Numeric421UnknownCommandParams> {
    static readonly COMMAND: string;
    static readonly PARAM_SPEC: MessageParamSpec<Numeric421UnknownCommandParams>;
    protected isResponseTo(originalMessage: Message): boolean;
    shouldEndResponseTo(originalMessage: Message): boolean;
}
