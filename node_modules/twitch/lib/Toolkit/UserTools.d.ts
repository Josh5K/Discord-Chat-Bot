import User from '../API/User/';
import ChannelPlaceholder from '../API/Channel/ChannelPlaceholder';
export declare type UserIdResolvable = string | User | ChannelPlaceholder;
export default class UserTools {
    static getUserId(user: UserIdResolvable): string;
    static toChannelName(user: string): string;
    static toUserName(channel: string): string;
}
