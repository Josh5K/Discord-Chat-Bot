import User, { UserData } from '../User/';
import Twitch from '../../';
export interface ChannelFollowData {
    created_at: string;
    notifications: boolean;
    user: UserData;
}
export default class ChannelFollow {
    private _data;
    _client: Twitch;
    constructor(_data: ChannelFollowData, client: Twitch);
    readonly user: User;
}
