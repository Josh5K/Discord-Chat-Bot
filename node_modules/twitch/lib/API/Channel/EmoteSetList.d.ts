import Twitch from '../../';
import { UniformObject } from '../../Toolkit/ObjectTools';
export interface Emote {
    code: string;
    id: number;
}
export declare type EmoteSetListData = UniformObject<Emote[]>;
export default class EmoteSetList {
    private _data;
    private _client;
    constructor(_data: EmoteSetListData, client: Twitch);
    findEmoteId(emoteName: string): number | undefined;
}
