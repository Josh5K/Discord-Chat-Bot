import Channel, { ChannelData } from '../Channel/';
import Twitch from '../../';
export interface UserFollowData {
    created_at: string;
    notifications: boolean;
    channel: ChannelData;
}
export default class UserFollow {
    private _data;
    _client: Twitch;
    constructor(_data: UserFollowData, client: Twitch);
    readonly channel: Channel;
}
