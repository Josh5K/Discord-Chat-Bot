import Twitch from '../../';
import User, { UserData } from './';
export interface UserBlockData {
    _id: string;
    updated_at: string;
    user: UserData;
}
export default class UserBlock {
    private _data;
    _client: Twitch;
    constructor(_data: UserBlockData, client: Twitch);
    readonly blockedUser: User;
}
