import Twitch from '../../';
import Channel, { ChannelData } from '../Channel/';
export interface StreamPreviewURLList {
    small: string;
    medium: string;
    large: string;
    template: string;
}
export interface StreamData {
    _id: string;
    game: string;
    viewers: number;
    video_height: number;
    average_fps: number;
    delay: number;
    created_at: string;
    is_playlist: boolean;
    preview: StreamPreviewURLList;
    channel: ChannelData;
}
export declare type StreamType = 'live' | 'playlist' | 'all';
export default class Stream {
    private _data;
    private _client;
    constructor(_data: StreamData, client: Twitch);
    readonly channel: Channel;
    readonly game: string;
    readonly viewers: number;
}
