import Twitch from '../';
export interface SubscriptionData {
    _id: string;
    sub_plan: string;
    sub_plan_name: string;
    created_at: string;
}
export default class Subscription {
    protected _data: SubscriptionData;
    protected _client: Twitch;
    constructor(_data: SubscriptionData, client: Twitch);
    readonly subPlan: string;
    readonly subPlanName: string;
}
